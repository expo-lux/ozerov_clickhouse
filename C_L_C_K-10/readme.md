Цель: установить `zookeeper` в `/opt/zookeeper`, запустить как демон. Данные хранить в `/var/lib/zookeeper`

На что потратил много времени:
- `ansible` роли требуют установленной `java`, хотя не везде это указано явно